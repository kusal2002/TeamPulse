info:
  name: Create Manager
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |-
      {
        "email": "manager@gmail.com",
        "password": "manager123",
        "name": "Manager",
        "role": "MANAGER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
